<Page
    x:Class="Tello.App.UWP.MainPage"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tello.App.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <!-- flight control row -->    
            <RowDefinition Height="Auto" />
            <!-- flight state 6 pack row -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- flight control row -->
        <Grid Grid.Row="0" Background="Khaki">
            <TextBlock>flight controls go here</TextBlock>
        </Grid>

        <!-- flight state 6 pack -->
        <Grid Grid.Row="1" Background="SteelBlue"> 
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- first row -->
            <StackPanel Grid.Row="0" Grid.Column="0">
                <TextBlock>speed indicator</TextBlock>
                <TextBlock></TextBlock>
            </StackPanel>
            <StackPanel Grid.Row="0" Grid.Column="1">
                <TextBlock>attitude indicator</TextBlock>
            </StackPanel>
            <StackPanel Grid.Row="0" Grid.Column="3">
                <TextBlock>altitude indicator</TextBlock>
            </StackPanel>

            <!-- second row -->
            <StackPanel Grid.Row="1" Grid.Column="0">
                <TextBlock>lateral speed indicator (turn coordinator)</TextBlock>
            </StackPanel>
            <StackPanel Grid.Row="1" Grid.Column="1">
                <TextBlock>heading indicator</TextBlock>
            </StackPanel>
            <StackPanel Grid.Row="1" Grid.Column="3">
                <TextBlock>vertical speed indicator</TextBlock>
            </StackPanel>

            <!-- third row -->
            <StackPanel Grid.Row="2" Grid.Column="0">
                <TextBlock>hobbs meter</TextBlock>
            </StackPanel>
            <StackPanel Grid.Row="2" Grid.Column="1">
                <TextBlock>fuel (battery)</TextBlock>
            </StackPanel>
            <StackPanel Grid.Row="2" Grid.Column="3">
                <TextBlock>temperature</TextBlock>
            </StackPanel>            
        </Grid>


        <!--<controls:RadialGauge x:Name="RadialGaugeControl" Value="70" Minimum="0" 
	        Maximum="180" TickSpacing="20" ScaleWidth="26" Unit="Units" TickBrush="Gainsboro"
	        ScaleTickBrush="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	        NeedleWidth="5" TickLength="18">
        </controls:RadialGauge>-->

    </Grid>
</Page>
